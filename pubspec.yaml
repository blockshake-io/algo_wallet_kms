name: algo_wallet_kms
description: A secure KMS wallet for Flutter
publish_to: 'none'
version: 0.5.0
homepage: blockshake.io

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # verified libraries
  crypto: 3.0.2

  # unverified libraries
  algorand_dart: ^2.0.0-dev.11

  cryptography: 2.5.0

  secure_storage:
    git:
      url: git@github.com:blockshake-io/secure_storage.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
